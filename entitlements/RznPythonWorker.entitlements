<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!--
      Sandboxed helper for Secure Python execution.

      Goals:
      - OS-level App Sandbox boundary for the worker process.
      - No network entitlements (baseline exfiltration guardrail).
      - Filesystem surface limited by the sandbox container + security-scoped bookmarks.
    -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Required for security-scoped bookmark activation (Approved Folders). -->
    <key>com.apple.security.files.bookmarks.app-scope</key>
    <true/>

    <!-- IMPORTANT: intentionally omit com.apple.security.network.* entitlements -->
  </dict>
</plist>

